import { defineConfig } from "vite";

// Le site est publié sous https://wald52.github.io/Synth-se-r-formes/
// `base` doit donc correspondre exactement au nom du dépôt (sensible à la casse).
export default defineConfig({
  base: "/Synth-se-r-formes/",
});
